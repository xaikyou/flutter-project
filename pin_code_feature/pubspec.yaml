name: pin_code_feature
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Support UI
  google_fonts:
  pin_code_fields:
  flutter_screenutil:

  # Camera
  camera: ^0.11.0+2
  path_provider: ^2.1.4
  path: ^1.9.0

  # Service locator
  get_it:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
